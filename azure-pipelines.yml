trigger:
- main

pr:
- main

pool:
  name: 'Default'

variables:
- group: budget-tracker-secrets
- name: npm_config_cache
  value: $(Pipeline.Workspace)/.npm
- name: PIP_CACHE_DIR
  value: $(Pipeline.Workspace)/.pip

stages:

# ── STAGE 1: TEST & LINT ──────────────────────────────────────────────────────
- stage: Test
  displayName: 'Test & Lint'
  jobs:

  - job: Frontend_Test
    displayName: 'Frontend — Lint'
    steps:
    - task: Cache@2
      inputs:
        key: 'npm | "$(Agent.OS)" | app-frontend/package-lock.json'
        restoreKeys: 'npm | "$(Agent.OS)"'
        path: $(npm_config_cache)
      displayName: 'Cache npm'

    - script: |
        cd app-frontend
        npm ci
        npm run lint
      displayName: 'Install & Lint'

  - job: Backend_Test
    displayName: 'Backend — Check & Test'
    steps:
    - task: Cache@2
      inputs:
        key: 'pip | "$(Agent.OS)" | backend/requirements.txt'
        restoreKeys: 'pip | "$(Agent.OS)"'
        path: $(PIP_CACHE_DIR)
      displayName: 'Cache pip'

    - script: |
        python3 -m venv backend/venv
        source backend/venv/bin/activate
        pip install --cache-dir $(PIP_CACHE_DIR) -r backend/requirements.txt
      displayName: 'Install dependencies'

    # Check & Test zusammengefasst, um Variablen-Redundanz zu vermeiden
    - script: |
        source backend/venv/bin/activate
        cd backend
        python manage.py check
        python manage.py test
      displayName: 'Django Check & Run Tests'
      env:
        DJANGO_SECRET_KEY: $(DJANGO_SECRET_KEY)
        GOOGLE_CLIENT_ID: $(GOOGLE_CLIENT_ID)
        GOOGLE_SECRET: $(GOOGLE_SECRET)
        POSTGRES_DB: $(POSTGRES_DB)
        POSTGRES_USER: $(POSTGRES_USER)
        POSTGRES_PASSWORD: $(POSTGRES_PASSWORD)
        POSTGRES_HOST: $(POSTGRES_HOST)
        POSTGRES_PORT: $(POSTGRES_PORT)


# ── STAGE 2: DEPLOY (BUILD & PUSH DOCKER IMAGES) ──────────────────────────────
- stage: Deploy
  displayName: 'Build & Push Docker Images'
  dependsOn: Test
  # Wird nur ausgeführt, wenn Tests bestanden sind UND es der main-Branch ist (kein PR-Build)
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
  jobs:

  - deployment: Push_Images
    displayName: 'Build & Push to Docker Hub'
    environment: 'production'
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self

          - task: Docker@2
            displayName: 'Login to Docker Hub'
            inputs:
              command: login
              containerRegistry: 'DockerHub-Connection'

          - script: |
              docker build \
                -t eduardgohl/budget-tracker-frontend:$(Build.BuildId) \
                -t eduardgohl/budget-tracker-frontend:latest \
                -f app-frontend/Dockerfile app-frontend
              docker push eduardgohl/budget-tracker-frontend:$(Build.BuildId)
              docker push eduardgohl/budget-tracker-frontend:latest
            displayName: 'Build & Push Frontend'

          - script: |
              docker build \
                -t eduardgohl/budget-tracker-backend:$(Build.BuildId) \
                -t eduardgohl/budget-tracker-backend:latest \
                -f backend/Dockerfile backend
              docker push eduardgohl/budget-tracker-backend:$(Build.BuildId)
              docker push eduardgohl/budget-tracker-backend:latest
            displayName: 'Build & Push Backend'


# ── STAGE 3: UPDATE MANIFEST REPO ─────────────────────────────────────────────
- stage: UpdateManifests
  displayName: 'Update Kubernetes Manifests'
  dependsOn: Deploy
  condition: succeeded()
  jobs:
  - job: PatchImageTags
    displayName: 'Patch image tags in manifest repo'
    steps:
    - checkout: none

    - script: |
        # Manifest-Repo klonen (HTTPS mit PAT)
        REPO_URL="https://$(MANIFEST_REPO_PAT)@dev.azure.com/edodevops0169/TackYourBudget/_git/app-manifests"
        git clone "$REPO_URL" manifests
        cd manifests

        # Image-Tags auf aktuelle Build-ID setzen (mit k8s/ Pfad!)
        sed -i "s|eduardgohl/budget-tracker-frontend:[^ ]*|eduardgohl/budget-tracker-frontend:$(Build.BuildId)|g" k8s/frontend.yaml
        sed -i "s|eduardgohl/budget-tracker-backend:[^ ]*|eduardgohl/budget-tracker-backend:$(Build.BuildId)|g" k8s/backend.yaml

        # Commit & Push
        git config user.email "pipeline@devops"
        git config user.name "Azure Pipeline"
        git add k8s/frontend.yaml k8s/backend.yaml
        git commit -m "chore: update image tags to $(Build.BuildId) [skip ci]"
        git push
      displayName: 'Update image tags and push'
      env:
        # Dies stellt sicher, dass das Secret sicher an das Skript übergeben wird
        MANIFEST_REPO_PAT: $(MANIFEST_REPO_PAT)